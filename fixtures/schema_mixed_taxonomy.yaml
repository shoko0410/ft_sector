{
  "contract_version": "1.0",
  "output_schema": {
    "fields": [
      "security_id",
      "ticker",
      "country",
      "taxonomy_label"
    ]
  },
  "proxy_quality_thresholds": {
    "max_unknown_ratio": 0.1,
    "min_high_confidence_ratio": 0.7,
    "max_low_confidence_ratio": 0.15
  }
}
